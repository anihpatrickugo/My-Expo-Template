import { darkColors, lightColors } from "./colors";
import { fontFamilies, fontSizes } from "./font";
import { border, icon, padding, radius, spacing } from "./sizes";

export const Theme = {
  colors: {
    dark: darkColors,
    light: lightColors,
  },
  font: {
    family: fontFamilies,
    size: fontSizes,
  },
  size: {
    border,
    icon,
    padding,
    radius,
    spacing,
  },
};
